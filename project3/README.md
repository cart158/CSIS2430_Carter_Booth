# CSIS2430_Carter_Booth

This project was made using Apache NetBeans and coded using Java 21.

Just like Project 1 and 2 the program for Project 3 can be run through the Optimal_Selection.jar file.
Unlike the other two projects the output is a text file called output.txt, the file is generated in the same folder as one of the 2 Optimal_Selection.jar files.
There already is an output.txt file in the Optimal_Selection folder.